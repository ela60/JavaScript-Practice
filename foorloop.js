//nested for loop multiplication table of 2,3,4,5
for (var x = 2; x <= 8; x++){
    for (var y = 1; y <= 10; y++){
        console.log(x+" X " + y +" = " + (x*y))
    }
    console.log("\n"+ x +" number of namta"+"\n")
}